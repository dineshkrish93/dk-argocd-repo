apiVersion: v1
kind: Namespace
metadata:
  name: namespace-dineshkrish93

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-dineshkrish93-omniverse-viewer
  namespace: namespace-dineshkrish93
  labels:
    app: deployment-dineshkrish93-omniverse-viewer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deployment-dineshkrish93-omniverse-viewer
  template:
    metadata:
      labels:
        app: deployment-dineshkrish93-omniverse-viewer
    spec:
      containers:
        - name: container-dineshkrish93-kit-app
          image: dineshkrish93/dk-public-repo:kit-app-editor-1-streaming
          imagePullPolicy: IfNotPresent
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "all"
          resources:
            limits:
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm

        - name: container-dineshkrish93-web-viewer
          image: dineshkrish93/dk-public-repo:web-viewer-sample-1.5.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80

      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 4Gi

---
apiVersion: v1
kind: Service
metadata:
  name: service-dineshkrish93-omniverse-viewer
  namespace: namespace-dineshkrish93
  labels:
    app: deployment-dineshkrish93-omniverse-viewer
spec:
  type: NodePort
  selector:
    app: deployment-dineshkrish93-omniverse-viewer
  ports:
    - name: web-viewer
      port: 80
      targetPort: 80
      nodePort: 30080
    - name: webrtc-signaling
      port: 49100
      targetPort: 49100
      protocol: TCP
      nodePort: 30100
