apiVersion: v1
kind: Namespace
metadata:
  name: namespace-testuser10

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-testuser10-omniverse-viewer
  namespace: namespace-testuser10
  labels:
    app: deployment-testuser10-omniverse-viewer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deployment-testuser10-omniverse-viewer
  template:
    metadata:
      labels:
        app: deployment-testuser10-omniverse-viewer
    spec:
      hostNetwork: true
      containers:
        - name: container-testuser10-kit-app
          image: dineshkrish93/dk-public-repo:kit-app-editor-1-streaming
          imagePullPolicy: IfNotPresent
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "all"
            - name: NVDA_KIT_ARGS
              value: "--/exts/omni.kit.livestream.app/primaryStream/signalPort=49104 --/exts/omni.kit.livestream.app/primaryStream/streamPort=48002"
          resources:
            limits:
              nvidia.com/gpu: "1"
          volumeMounts:
            - name: dshm
              mountPath: /dev/shm

        - name: container-testuser10-web-viewer
          image: dineshkrish93/dk-public-repo:web-viewer-sample-1.5.2
          imagePullPolicy: Always
          ports:
            - containerPort: 8084
          env:
            - name: SIGNALING_PORT
              value: "49104"
            - name: WEB_PORT
              value: "8084"

      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 4Gi

---
apiVersion: v1
kind: Service
metadata:
  name: service-testuser10-omniverse-viewer
  namespace: namespace-testuser10
  labels:
    app: deployment-testuser10-omniverse-viewer
spec:
  type: NodePort
  selector:
    app: deployment-testuser10-omniverse-viewer
  ports:
    - name: web-viewer
      port: 80
      targetPort: 8084
